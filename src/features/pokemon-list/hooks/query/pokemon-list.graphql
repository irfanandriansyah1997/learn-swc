query GetPokemonList($limit: Int, $offset: Int) {
  pokemons(limit: $limit, offset: $offset) {
    count
    nextOffset
    prevOffset
    params
    status
    message
    results {
      id
      image
      name
    }
  }
}
